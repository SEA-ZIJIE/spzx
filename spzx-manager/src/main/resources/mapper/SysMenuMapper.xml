<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.spzx.manager.mapper.SysMenuMapper">
    <resultMap id="sysMenuMap" type="com.atguigu.spzx.model.entity.system.SysMenu" autoMapping="true">

    </resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,parent_id,title,component,sort_value,status,create_time,update_time,is_deleted
    </sql>
    <select id="selectAll" resultMap="sysMenuMap">
        select
        <include refid="columns"/>
        from `db-spzx`. sys_menu
        where
        is_deleted = 0
        order by sort_value
    </select>
<!--    //添加菜单-->

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into `db-spzx`.sys_menu(
                id,
                parent_id,
                title,
                component,
                sort_value,
                status)
        values (#{id},
                #{parentId},
                #{title},
                #{component},
                #{sortValue},
                #{status})
    </insert>
<!--    //修改菜单-->
<update id="updateById">
    update `db-spzx`.sys_menu set
    <if test="parentId ! = null and parentId ！ = ‘’ ">
        parent_id = #{parent_id},
    </if>
    <if test="title ! = null and title ! = '' ">
        title = #{title},
    </if>
    <if test="component != null and component ! = '' ">
        component = #{component},
    </if>
    <if test="sortValue != null">
        sort_value = #{sortValue},
    </if>
    <if test="status != null">
        status = #{status},
    </if>
    update_time = now()
    where id = #{id}
</update>
<!--    //删除表单-->
    <select id="countByParentId" resultType="Integer">
        select count(id)
        from `db-spzx`. sys_menu
        where
            parent_id = #{parentId}
          and is_deleted = 0
    </select>

    <update id="deleteById">
        update `db-spzx`. sys_menu set
                            update_time = now() ,
                            is_deleted = 1
        where
            id = #{id}
    </update>

    <select id="selectListByUserId" resultMap="sysMenuMap">
        select distinct m.*
            from `db-spzx`.sys_menu m
        inner join `db-spzx`.sys_role_menu rm on rm.menu_id =  m.id
        inner join `db-spzx`.sys_user_role ur on ur.role_id = rm.role_id
        where ur.user_id = #{userId} AND m.is_deleted = 0
    </select>
    <select id="selectById" resultMap="sysMenuMap">
        select <include refid="columns" /> from `db-spzx`. sys_menu where id = #{id}
    </select>
</mapper>