<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.spzx.manager.mapper.SysRoleMenuMapper">

<!--    //分配菜单：查询菜单-->

    <select id="findSysRoleMenuByRoleId" resultType="long">
        select menu_id

        from `db-spzx`.sys_role_menu
        where role_id = #{roleId}
          and is_deleted = 0
          and is_half = 0


    </select>
<!--    // 获取菜单的id-->
<select id="deleteByRoleId">


delete from `db-spzx`.sys_role_menu where role_id = #{roleId}
</select>
    <insert id="doAssign">
    insert into `db-spzx`. sys_role_menu (
    role_id,
    menu_id,
    create_time , update_time , is_deleted , is_half
    ) values
    <foreach collection="menuIdList" item="menuInfo" separator=",">
        (#{roleId} , #{menuInfo.id} , now() , now() , 0 , #{menuInfo.isHalf})
    </foreach>
</insert>
    <select id="updateSysRoleMenuIsHalf">
        update `db-spzx`. sys_role_menu srm set srm.is_half = 1 where menu_id = #{menuId}
    </select>

</mapper>